<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="300dp">

        <com.google.android.gms.maps.MapView
            android:id="@+id/new_post_map"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:tint="@android:color/white" />
    </LinearLayout>

    <Button
        android:id="@+id/new_post_current_location_btn"
        style="@style/BorderlessButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:contentDescription="@string/new_post_location"
        android:drawableTop="@drawable/ic_location"
        android:text="@string/current_location_label" />

    <EditText
        android:id="@+id/new_post_address"
        style="@style/NormalText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="@string/new_post_location"
        android:hint="@string/new_post_location"
        android:inputType="text"
        tools:targetApi="o" />

</LinearLayout>